# ---------------------------------------------------------------
# ---------------------------------------------------------------
[stable]
version=1.6.6
url=https://albar965.github.io/release/2017/10/09/littlenavmap-stable-166-released.html
downloadwin=https://github.com/albar965/littlenavmap/releases/download/v1.6.6/LittleNavmap-win-1.6.6.zip
downloadmac=https://github.com/albar965/littlenavmap/releases/download/v1.6.6/LittleNavmap-macOS-1.6.6.zip
downloadlinux=https://github.com/albar965/littlenavmap/releases/download/v1.6.6/LittleNavmap-linux-1.6.6.tar.gz
changelog=
    <p>This update includes several bugfixes, small GUI improvements, a revised manual and
    support for the OpenAir airspace format for X-Plane.
   </p>
  <h3>Changes from Release 1.6.4 to 1.6.6</h3>
    <p>Version 1.6.5 was an internal beta release.</p>
  <h4>Improvements</h4>
  <ul>
    <li>Added OpenAir airspace format compilation for X-PLane. Also reading airspace files from
      from <code>YOUR_DOCUMENTS_DIR/Little Navmap/X-Plane Airspaces</code>. You have to create the 
       <code>Little Navmap/X-Plane Airspaces</code> directory manually.</li>
    <li>Added new airspace types <code>Wave Window</code> and <code>Glider Prohibited</code>.</li>
    <li>Removed restrictions on user defined waypoint names. 
      Names are now adjusted according to flight plan format when saving.</li>
    <li>Enabled user defined waypoint names for X-Plane FMS format.</li>
    <li>Changed rating checkbox to drop down list in airport search.</li>
    <li>X-Plane 3D airport state is now considered when calculating rating.</li>
    <li>Added display of simulator type to connection status in statusbar.</li>
    <li>Airspace buttons are now disabled if no airspaces are loaded.</li>
    <li>Now clipping long airport and user waypoint names on map.</li>
    <li>Removed <code>no SimConnect</code> header label in connect dialog.</li>
    <li>Added display of AIRAC cycle to database dialog (X-Plane only).</li>
    <li>Added scenery file display for airspaces in information window.</li>
    <li>Added download button to update notification dialog.</li>
    <li>Better error handling and reporting of too old files with version &lt; 850 when loading X-Plane scenery. 
      Now checking X-Plane CIFP filenames before loading.</li>
    <li>Now showing dialog on certain crash types to avoid silent crash to desktop.</li>
    <li>Made loading of <code>add-on.xml</code> more robust for files with wrong encoding.</li>
    <li>Added coordinates to CSV export to clipboard from flight plan and search result tables.</li>
  </ul>
  <h4>Bug Fixes</h4>
  <ul>
    <li>Offline map themes <code>Atlas</code> and <code>Simple</code>: Fixed missing lakes in map.</li>
    <li>Fixed problem loading FS9 flight plans.</li>
    <li>Attempt to fix zoom distance creep while using moving map.</li>
    <li>Fixed error in airspace copy process from older database schemes where bounding rectangle was messed up. 
      This resulted in an <code>Too many objects</code> message in the statusbar.</li>
    <li>Solved problem with gaps between SIDs, STARs and the respective transitions resulting in wrong
      display and distance calculation.</li>
    <li>Fixed problem where STAR was not reloaded after database switch.</li>
    <li>Fixed issue where airspace display was not updated when changing cruise altitude with empty flight plan.</li>
    <li>Fixed user waypoint format for FLP so that it can be reloaded.</li>
    <li>Fixed confusing <code>0 errors</code> dialog appearing after loading X-Plane database successfully.</li>
    <li>Fixed <code>Connect on startup</code> checkbox state not saved in some cases.</li>
    <li>Fixed status bar connection indication remaining in disconnected state sometimes.</li>
    <li>Fixed potential crash on failing network connection.</li>
    <li><code>Calculate based on given altitude</code> is working again. Previously failed with 
      <code>Cannot find a route</code>.</li>
    <li>Now correctly considering minimum and maximum altitude ranges when calculating to avoid
      non-overlapping airway altitude ranges in one flight plan.</li>
    <li>Fixed error in flight plan saving logic where cancel did not stop saving.</li>
  </ul>

# ---------------------------------------------------------------
# ---------------------------------------------------------------
[beta]
version=1.6.5.beta
downloadwin=https://www.magentacloud.de/lnk/1wlR0mQy
downloadmac=https://www.magentacloud.de/lnk/2WlR01RH
downloadlinux=https://www.magentacloud.de/lnk/Sylx0Sl4
changelog=
 <p><b>Changes from 1.6.4 to 1.6.5.beta</b></p>
 <p><i>Airspace view will be reset to default.</i></p>
 <ul>
   <li>Added OpenAir airspace format compilation for X-PLane. Reading &quot;*.txt&quot; files from
     &quot;YOURXPLANE/Custom Data/Airspaces&quot;,
     &quot;YOURXPLANE/Resources/default data/airspaces&quot; and
     from &quot;YOURHOME/Documents/Little Navmap/X-Plane Airspaces&quot; where &quot;Documents&quot; is the documents directory in you local language.</li>
   <li>Added scenery file display for airspaces in information window.</li>
   <li>Changed rating checkbox to combo in airport search.</li>
   <li>X-Plane 3D airport state now considered when calculating rating.</li>
   <li>Added display of simulator type to connection status in statusbar.</li>
   <li>Added display of AIRAC cycle to database dialog (only X-Plane).</li>
   <li>Enabled user defined waypoint names for X-Plane FMS format.</li>
   <li>Removed restrictions on user defined waypoint names. Names are now adjusted according to save flight plan format.</li>
   <li>Added coordinates to CSV export by clipboard.</li>
   <li>Added download button to update notification dialog.</li>
   <li>Fixed user waypoint format for FLP so that it can be reloaded.</li>
   <li>Fixed status bar connection indication remaining in disconnected state sometimes.</li>
   <li>Fixed &quot;Connect on startup&quot; checkbox state not saved in some cases.</li>
   <li>Solved problem with gaps between SIDs, STARs and the respective transitions resulting in wrong display and distance calculation.</li>
   <li>Fixed problem where STAR was not reloaded after database switch in some cases.</li>
   <li>Cleanup of airspace management code to allow addition of more airspace types (&quot;Wave Window&quot; and &quot;Glider Prohibited&quot;).</li>
   <li>Now clipping long airport and waypoint names on map.</li>
   <li>Fixed issue where airspaces were not updated when changing cruise altitude with empty flight plan.</li>
   <li>Fixed potential crash on failing network connection.</li>
   <li>Fixed problem loading certain FS9 flight plans.</li>
   <li>Fixed confusing 0 error dialog appearing after loading X-Plane database successfully.</li>
   <li>Now checking X-Plane CIFP filenames before loading. Wrong names are reported.</li>
   <li>Better error handling and reporting of too old files with version < 850 when loading X-Plane scenery.</li>
   <li>Added handler for assertions/crashes showing dialog to avoid crash to desktop.</li>
   <li>Made loading of add-on.xml more robust for files with wrong encoding.</li>
   <li>Increased database minor version to 3.</li>
   <li>Removed &quot;no SimConnect&quot; header label in connect dialog.</li>
   <li>Fixed several small text errors in GUI.</li>
 </ul>
 </small>

# ---------------------------------------------------------------
# ---------------------------------------------------------------
[develop]
version=1.5.4.develop

downloadwin=https://www.magentacloud.de/lnk/h4FxUgf1
downloadmac=https://www.magentacloud.de/lnk/dYFRU7Nk
downloadlinux=https://www.magentacloud.de/lnk/q6lRUNb7

changelog=
 <ul>
   <li>
     Full support for X-Plane 11:
     <ul>
       <li>scenery database: Airports, aprons, taxiways, navaids, airways, procedures, </li>
       <li>AI traffic, </li>
       <li>weather, </li>
       <li>copy airspaces from FSX/P3D to X-Plane database,</li>
       <li>FMS file loading/saving and</li>
       <li>moving map (needs included Little Xpconnect plugin).</li>
     </ul>
   </li>
   <li>Automatic update check with settings in options dialog on tab startup.</li>
   <li>Speed limit for procedure segments.</li>
   <li>Fixed multiple problems with magnetic declination by using magdec.bgl file.</li>
   <li>Can now load FMS, FLP and FS9 ini-style PLN flight plan formats.</li>
   <li>Several problems fixed when loading FSX/P3D scenery databases and P3D v4 add-on.xml.</li>
   <li>Fixed missing encoding in saved PLN files.</li>
   <li>Shows startup positions now in the information window.</li>
   <li>Added clickspots in top labels to clear selections in procedure search and flight plan.</li>
   <li>Added donation link to help menu and about dialog (not active yet).</li>
 </ul>
